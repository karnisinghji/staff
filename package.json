{
  "name": "contractor-worker-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Contractor-Worker Platform Microservices",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && npm run dev",
    "install:services": "cd backend/services/auth-service && npm install && cd ../user-service && npm install && cd ../matching-service && npm install && cd ../communication-service && npm install && cd ../notification-service && npm install"
  },
  "workspaces": [
    "backend/services/*",
    "frontend"
  ],
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "author": "Your Name",
  "license": "MIT"
}