# Railway Environment Variables Template
# Copy these variables to each service in Railway dashboard

# === REQUIRED FOR ALL 5 SERVICES ===

# Database Configuration
DATABASE_URL=postgresql://neondb_owner:npg_AwN7nqtQOs8P@ep-proud-dew-adi1wdgd-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require

# JWT Configuration  
JWT_SECRET=contractor-worker-platform-super-secret-key-2025
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Environment
NODE_ENV=production

# CORS Configuration
CORS_ORIGIN=https://karnisinghji.github.io
ALLOWED_ORIGINS=https://karnisinghji.github.io,http://localhost:5173

# Port (Railway sets this automatically, but good to have)
PORT=3000

# === SERVICE-SPECIFIC NOTES ===

# Auth Service (Port 3001)
# - Handles user registration/login
# - Needs all above variables

# User Service (Port 3002) 
# - Handles user profiles
# - Needs all above variables

# Matching Service (Port 3003)
# - Handles contractor-worker matching  
# - Needs all above variables

# Communication Service (Port 3004)
# - Handles messaging between users
# - Needs all above variables
# - May need additional WebSocket config later

# Notification Service (Port 3005)
# - Handles push notifications
# - Needs all above variables
# - May need additional notification provider config later

# === AFTER DEPLOYMENT ===
# Update CORS_ORIGIN to include your Railway service URLs if needed