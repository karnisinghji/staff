{
  "name": "contractor-worker-platform",
  "version": "1.0.0",
  "description": "A microservices-based platform connecting contractors with workers",
  "main": "index.js",
  "scripts": {
  "dev": "concurrently \"npm run dev --prefix backend/services/auth-service\" \"npm run dev --prefix backend/services/user-service\" \"npm run dev --prefix backend/services/matching-service\" \"npm run dev --prefix backend/services/communication-service\" \"npm run dev --prefix backend/services/notification-service\"",
    "start": "docker-compose up",
    "stop": "docker-compose down",
    "clean": "docker-compose down -v --remove-orphans",
    "db:migrate": "cd database && npm run migrate",
    "db:seed": "cd database && npm run seed",
    "test": "jest",
    "lint": "eslint . --ext .ts,.js",
    "format": "prettier --write ."
  },
  "keywords": [
    "contractor",
    "worker",
    "matching",
    "microservices",
    "nodejs",
    "react-native"
  ],
  "author": "karan s",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "@typescript-eslint/parser": "^6.0.0",
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0",
    "typescript": "^5.0.0"
  },
  "workspaces": [
    "backend/services/*",
    "backend/api-gateway",
    "backend/shared",
    "frontend"
  ]
}