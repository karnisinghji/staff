// Thin HTTP adapter that will later map Express requests to use cases
// TODO: Wire real use cases via a composition root (container)
export class MatchingHttpController {
    // constructor(private readonly findWorkers: FindWorkersUseCase, ...) {}
}
